<%= form_with(model: licence, local: true) do |form| %>
  <% if licence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(licence.errors.count, "error") %> prohibited this licence from being saved:</h2>

      <ul>
      <% licence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :qtype_id %>
    <%= form.collection_select :ltype_id, Ltype.all, :id, :name, {promt: 'Choose a category'}, id: :licence_qtype, class: "form-control" %>
  </div>


  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :licence_number %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.text_field :expiration_date, id: :licence_expiration_date, type: "date" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
