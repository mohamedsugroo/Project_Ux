<%= form_with(model: qtype, local: true) do |form| %>
  <% if qtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qtype.errors.count, "error") %> prohibited this qtype from being saved:</h2>

      <ul>
      <% qtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :qtype_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
