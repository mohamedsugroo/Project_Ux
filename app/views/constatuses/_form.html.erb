<%= form_with(model: constatus, local: true) do |form| %>
  <% if constatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(constatus.errors.count, "error") %> prohibited this constatus from being saved:</h2>

      <ul>
      <% constatus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note, id: :constatus_note %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.text_field :created_at, id: :constatus_created_at %>
  </div>

  <div class="field">
    <%= form.label :updated_at %>
    <%= form.text_field :updated_at, id: :constatus_updated_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
